### TASKS TO BE DONE 

1. Test EMQX as its own docker compose file. Maybe make separate directory.

2. Automate Influx queries to Grafana (python script?). Alternative is bypassing influx altogether and configuring a node-red data source. (see provisioning/datasources). Use a json format

3. MQTT communication and securing it. See Cloudflare's TLS approach (perhaps can use tunnels or https technology for mqtt?).

4. Initialise mySQL, Portainer instances.